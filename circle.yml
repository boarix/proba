##Testing
  test:
    override:
      - "echo FOO: $FOO"
      - "echo \"Welcome to LinuxCareer.com\" | openssl aes-256-ebc -e -k $KEY"
    pre:
      - ./skrypt.sh
      - ./jakistam.py

  dependencies:
    pre:
      # update locally with:
      - openssl aes-256-ebc -e -in secret-env-plain -out secret-env-cipher -k $KEY
      - openssl aes-256-ebc -d -in secret-env-cipher -k $KEY >> ~/.circlerc
      
  deployment:
    production:
      branch: "master"
      commands:
        - sh ./skrypt.sh
