##Testing
  test:
    override:
      - "echo FOO: $FOO"
    pre:
      - ./skrypt.sh
      - ./jakistam.py

  dependencies:
    pre:
      # update locally with:
<<<<<<< HEAD
      - openssl aes-256-cbc -e -in secret-env-plain -out secret-env-cipher -k $KEY
      # openssl aes-256-cbc -d -in secret-env-cipher -k $KEY >> ~/.circlerc
=======
      # openssl aes-256-cbc -e -in secret-env-plain -out secret-env-cipher -k $KEY
      - openssl aes-256-cbc -d -in secret-env-cipher -k $KEY
>>>>>>> 40d2ce0f2eed8734e981d710a6778ef8fb537425
      
  deployment:
    production:
      branch: "master"
      commands:
        - sh ./skrypt.sh
