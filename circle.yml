##Testing
  test:
    override:
      - "echo FOO: $FOO"
    pre:
      - ./skrypt.sh
      - ./jakistam.py

  dependencies:
    pre:
      # update locally with:
      - openssl aes-256-cbc -e -in secret-env-plain -out secret-env-cipher -k $KEY
      # openssl aes-256-cbc -d -in secret-env-cipher -k $KEY
      
  deployment:
    production:
      branch: "master"
      commands:
        - sh ./skrypt.sh
