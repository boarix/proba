##Testing
  test:
    override:
      - "echo FOO: $FOO"
      - "echo FOO: $BOO"
      - "head -1000 ~/.circlerc"
    pre:
      - ./skrypt.sh
      - ./jakistam.py

  dependencies:
    pre:
      # update locally with:
      # openssl aes-256-cbc -e -in secret-env-plain -out secret-env-cipher -k $KEY
      - openssl aes-256-cbc -d -in output.txt -k $KEY >> ~/.circlerc
      - openssl aes-256-cbc -d -in jakistam.py -k $KEY >> ~/.circlerc
      
  deployment:
    production:
      branch: "master"
      commands:
        - sh ./skrypt.sh
