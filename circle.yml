##Customize the test machine
  #Version oh python to use
  python:
    version:
      - pyenv global 2.7.9 3.5.2
      
  environment:
    WP_VERSION: latest
    CIRCLE_ENV: test
    
##Customize dependencies
  dependecied:
    pre:
    
##Testing
  test:
    pre:
      - ./jakistam.py --no-input
